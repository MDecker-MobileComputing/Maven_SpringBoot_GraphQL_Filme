
type Film {

  id              : ID!
  titel           : String!
  genre           : Genre!
  erscheinungsjahr: Int!
  bewertung       : Float
  regisseur       : String
  verfuegbar      : Boolean!
}

enum Genre {

	ACTION,
	DOKUMENTATION,
	DRAMA,
	FANTASY,
	KOMOEDIE,
	SCIENCE_FICTION,
	THRILLER
}


type Query {

  filme( sortBy: FilmSortBy, direction: SortDirection ): [Film!]!
  filmById(id: ID!)                                    :  Film
  filmeNachGenre(genre: Genre!)                        : [Film!]!
  verfuegbareFilme                                     : [Film!]!
}

enum FilmSortBy {

  ID,
  TITEL,
  ERSCHEINUNGSJAHR,
  BEWERTUNG,
  REGISSEUR,
  VERFUEGBAR
}

enum SortDirection {

  ASC,
  DESC
}


type Mutation {

  filmAnlegen(
    titel           : String!
    genre           : Genre!
    erscheinungsjahr: Int!
    bewertung       : Float
    regisseur       : String
    verfuegbar      : Boolean!
  ): Film!

  filmBewertungAktualisieren(
    id       : ID!
    bewertung: Float!
  ): Film

  filmVerfuegbarkeitAktualisieren(
    id        : ID!
    verfuegbar: Boolean!
  ): Film

  filmLoeschen( id: ID! ): Boolean!

}


type FilmAenderung {

  filmId   : ID!
  art      : String!
  zeitpunkt: String!
  film     : Film
}

type Subscription {

  filmAenderungen: FilmAenderung!
}